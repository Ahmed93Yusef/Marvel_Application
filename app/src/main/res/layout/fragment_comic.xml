<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.example.marvel_application.ui.comic.ComicViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.comic.ComicFragment">

        <TextView
            android:id="@+id/text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:layout_marginBottom="50dp"
            app:layout_constraintTop_toTopOf="parent"
            android:text="@{viewModel.result.toData().name}"
            android:textSize="42sp"/>

        <ImageView
            android:id="@+id/image_characters"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:ImageUrl="@{viewModel.result.toData().imageUrl}"
            app:layout_constraintTop_toBottomOf="@id/text"
            app:layout_constraintBottom_toBottomOf="parent"
          />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>